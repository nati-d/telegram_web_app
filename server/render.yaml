services:
  - type: web
    name: telegram-web-app
    env: go
    buildCommand: go build -o app .
    startCommand: ./app
    envVars:
      - key: PORT
        value: 8080
    build:
      env:
        GO_VERSION: 1.25.3
